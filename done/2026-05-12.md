# 2026-05-12 done

## fleet-director
- [x] D07-prereq triad: 종료 (PASS) — bc2bbbf + 31a3635 origin/main 안착
  - D05#8 spawn-claude.sh jq missing-status.json guard (`cycle=$(jq ... 2>/dev/null || echo "?")`)
  - D05#13 preflight.sh per-task/per-device capability check (claude CLI 무조건 + "github" declared device 한정 gh auth 검사)
  - D05#14 post-action-scan.sh large-diff bucketing (lines > 10000 → `git diff -G` stream)
  - D06#1 stale-recover.sh race-loser leftover 자동 회수 + `release_losing_recover` 함수
  - D06#2 stale-recover.sh git pull/push 실패 graceful handling
  - D06#7 stale-recover.sh `rm -rf || continue` per-iteration resilience
- [x] D05#6 SIGPIPE family 4th/5th instance close — fix-forward 31a3635 (post-action capture-then-test + stale-recover here-string)
- [x] spec-reviewer subagent dispatch (양 기기 parity 검증 패턴) — bc2bbbf 6/6 PASS
- [x] code-quality-reviewer subagent dispatch — bc2bbbf PASS (+ SIGPIPE non-blocking flag, 후속 empirical 검증으로 active blocker 격상)
- [x] re-review combined subagent dispatch (31a3635) — SPEC PASS / SIGPIPE empirical repro+fix 검증 / LOCALITY+SIMPLICITY OK
- [x] Targeted SIGPIPE repro: PASS (pattern A buggy=0 / B,C fixed=1)
- [x] Full tests: 9/10 PASS with known claim-race env failure (mac-mini local main divergence, reset 보류)
- [x] tests/preflight.test.sh case 9/10 신규 (D05#13 gh capability gate matrix)
- [x] tests/stale-recover.test.sh D06#1 leftover scenario 신규

## process / 사고 기록
- [x] review 전 origin push 사고 — test harness 가 implementer commit 까지 끌고 올라감
  - 원인 고정: 검증용 fixture push 가 branch HEAD 까지 끌어올림
  - force-push/revert 대신 fix-forward 선택 (공유 branch 파괴 위험 > leak 심각도)
- [x] follow-up todo 등록 — fleet-state test harness push 격리 (D07 cron 전 blocker 로 승격)
- [x] 이슈 로그: `~/.claude/skills/issues/2026-05-12-fleet-state-tests-auto-push-leak.md` (claude-skills 43bcf55)

## 인프라/운영
- [x] 5/11 분 insta-post-general 캐러셀 수동 발행 (https://www.instagram.com/p/DYNtzX_HNUY/) — 23:50 cron `EXTRA[@]: unbound variable` (set -u + 빈 배열) 사고 후 복구
- [x] insta-post-general/run.sh fix 적용 확인 (a8d4596, EXTRA 제거 + --force 를 PNG_ARGS 통합)

## fleet test push 격리 (오후)
- [x] tests/*.test.sh ephemeral clone 패턴 적용 — `mktemp -d + git clone + trap rm -rf` prologue, `cd "$EPHEMERAL_DIR/repo"` body
- [x] claim-single.test.sh + stale-recover.test.sh ephemeral 적용 + verifier PASS (local main `e37399e` fake commit isolated, origin grep = 0)
- [x] claim-race.test.sh 도 ephemeral 적용 — mac-mini SSH 단계는 production fleet 그대로
- [x] claim-race blocked by mac-mini orphan commits (10 ahead/30 behind) — D07 start gate 의 mac-mini reset 옵션 대기

## 광고 두 앱 iPhone 빌드 + ASC 심사 제출
- [x] 광고 앱 식별 (google_mobile_ads pubspec grep) — 더치페이 + 약먹자
- [x] mac-mini USB iPhone17 release 빌드 시도 — 3차 사이클 (flutter install 단독 → fail / debug build → JIT crash / release build → 성공)
- [x] ssh 비대화 shell PATH 함정 발견 + `export PATH=/opt/homebrew/bin:$PATH` 박음
- [x] 더치페이 Info.plist `GADApplicationIdentifier` 누락 발견 (앱 launch crash 원인)
- [x] AdMob 콘솔 → 더치페이 iOS 신규 앱 + dutch_banner_ios 배너 광고단위 발급
- [x] Info.plist `GADApplicationIdentifier` 신규 추가 + ads_service.dart `_realIosBannerUnitId` 운영 ID 교체 (3c5daa7)
- [x] xcrun devicectl device install — flutter install hang 우회
- [x] 강대종 폰 더치페이 + 약먹자 광고 정상 load ack
- [x] 더치페이 1.0.2+5 → 1.0.3+6 bump (39717cc)
- [x] 약먹자 1.0.1+2 → 1.0.2+3 bump (275b285)
- [x] flutter build ipa --release 둘 다 PASS (Xcode build 56.8s + 33.3s)
- [x] xcrun altool upload SUCCEEDED — dutch 22.6MB (Delivery UUID 52fc4569-...) + yak 24.2MB (f66e6879-...)
- [x] asc-submit.py 작성 — Python 3.9 호환 ASC API 자동 submit (Step 1~9, 재사용 가능)
- [x] 더치페이 v1.0.3 ASC submit — reviewSubmission df48b9f9-... SUBMITTED (AFTER_APPROVAL)
- [x] 약먹자 v1.0.2 ASC submit — reviewSubmission b6a60ee5-... SUBMITTED (AFTER_APPROVAL)
- [x] ASC build VALID 폴링 — yak 첫 polling 만에 VALID (60s)

## 인프라/이슈/lesson 신규
- [x] 이슈: `~/.claude/skills/issues/2026-05-12-dutchpay-gad-application-identifier-missing.md` — AdMob 신규 앱 시 Info.plist GAD key 누락 → launch crash
- [x] lesson: `~/.claude/skills/submit-app/lessons/ios-flutter-install-needs-build-first.md` — flutter install 단독 호출은 빌드 안 함
- [x] lesson: `~/.claude/skills/submit-app/lessons/ios-asc-uses-non-exempt-encryption.md` — ASC API submit 시 build PATCH 필수

## 할일 완료
- [x] 🍎 🛠️ fleet-director D07-prereq triad 단일 사이클 — D07 cron 통합 전 multi-host safety 6항목 처리 후 D07 시작. 항목: D05#8 (spawn-claude jq guard) / D05#13 (preflight capability check) / D05#14 (post-action large-diff guard) / D06#1 (stale-recover multi-device race + release_losing_recover) / D06#2 (recover.sh pull/push 실패 처리) / D06#7 (rm -rf per-iteration resilience). spec: `~/.claude/plans/plan-fleet-director-2026-05-10.md` Day 7. (추가: 2026-05-12, 완료: 2026-05-12 — `bc2bbbf feat(D07-prereq): multi-host safety triad` + `31a3635 fix: avoid pipefail SIGPIPE in prereq guards` 양 SHA origin/main 안착. spec-reviewer 6/6 PASS, code-quality-reviewer PASS, re-review combined PASS. Targeted SIGPIPE repro: PASS. Full tests: 9/10 PASS with known claim-race env failure (mac-mini local main divergence, reset 보류). SIGPIPE family 4th/5th instance close. process 사고: test harness 가 implementer commit 끌고 origin 끌어올림 → force-push/revert 대신 fix-forward 선택. mac-mini reset 별도 ack 대기.)
- [x] 🍎 💰 더치페이 AdMob 배너 단위 ID 교체 — ✨ 자동 매칭 완료 (goodnight step 1.5). AdMob 콘솔 신규 발급 (App ID ~7679626181 + Banner /6556140410) + Info.plist GAD key + ads_service.dart 교체 + 1.0.3+6 ASC 심사 제출 SUBMITTED.

# 2026-06-07 작업일지 v1.0.1 (밤 추가분)

> v1.0.0(araseo-voice Siri 음성 비서 + 함대 "성급한 단정 금지" 정비) 발행 이후, 같은 날 밤에 더해진 한 갈래. 39일 만에 출시된 한줄일기 안드로이드를 계기로, 6/4 이후 밀려 있던 뉴스레터를 하룻밤에 5편 몰아 발행했다.

## 1. 한줄일기 안드로이드 출시 — 왜 39일이 걸렸나 (기록 추적)

아니키 "오늘 한줄일기 출시했대, 왜 오래 걸렸는지 분석" → 메모리·git 으로 타임라인 복원.

- **iOS**: 4/28 첫 제출(패키지명 `com.ssamssae.hanjul` 충돌 → `com.daejongkang.hanjul` 전환) → 4/29 China Deep Synthesis 리젝 → 4/30 중국 제외 재제출(ASC API 우회: 옛 sub cancel→새 sub attach→submit) → 4/30 13:29 승인. 재제출→승인 **2시간 23분**, ₩1,900 LIVE.
- **안드로이드 (39일)**: 심사가 아니라 "절차의 대기"가 범인.
  1. 패키지명 충돌 (옛 debug.keystore SHA 선점) → 도메인 전환
  2. Play Console "관리 게시(Managed Publishing) ON" 함정 — 5/2 알파 검토 통과했으나 수동 게시 토글 대기로 14일 stuck
  3. 신규 개발자 프로덕션 게이트 — 비공개 테스트 12명+14일 충족 후 5/21 프로덕션 액세스 신청 → "7일 내 결과" 안내가 약 한 달 → 오늘 6/7 출시
- 핵심: iOS의 장벽은 "풀 문제"(빠름), 안드로이드는 "기다릴 절차"(느림). 이 비대칭을 ep27 로 작성.

## 2. 뉴스레터 ep27~31 5편 작성 + Substack/홈페이지 발행

6/4 flock편(ep26) 이후 미발행. 6/5~6/7 worklog 기반 5편 신규 작성:

| ep | 제목 | Substack |
|----|------|----------|
| 27 | iOS는 두 시간, 안드로이드는 서른아홉 날 — 한줄일기 | /p/ios |
| 28 | 다섯 대를 일 시키기 전에, 켜고 끄는 스위치부터 (자율워커 4엔진) | /p/29a |
| 29 | 스튜디오의 얼굴과, 받을 생각 없는 전화번호 (로고+070) | /p/d7e |
| 30 | 보고가 엉뚱한 데로 가는 줄 알았는데, 이름표만 틀렸다 (라벨버그) | /p/8f6 |
| 31 | "에어컨 꺼줘"가 안 되던 날, AI가 두 번 헛다리 (Siri stale) | /p/ai |

- Substack 발행 = Playwright MCP, 2단계 모달(Send to everyone now → Publish without buttons) 통과.
- 홈페이지 sync = `sync_from_substack.py` ep27~31 cache+index.json 갱신, 커밋 `cf42e8b`.

## 3. 인사이트 1편 (데스크탑 경유 요청)

- 데스크탑(🖥) codex 가 본진 경유로 인사이트 요청: 김미경 대표 "AI 플러스 휴먼" (책과삶, 25분 49초).
- 자막 추출 → 픽업 7개 + 용어 7개 압축 → publish. 커밋 `2c33cd6`.
- **git 충돌 처리**: publish.sh commit 직후 push 가 다른 노드의 동시 push(45d873a)로 rejected. pull --rebase 가 post-commit hook(ai-glossary regenerate)과 충돌 → `git reset --hard origin/main` 으로 정렬 후 재발행. ep 소스 6파일은 untracked 라 보존. PII guard(스튜디오 공식 이메일, 기존 게시분)는 OVERRIDE 로 통과.

## 4. 네이버 블로그 ep27 발행 — SmartEditor paste 디버깅

- 제목은 `setDocumentTitle` API 로 정상 입력. 본문 paste 가 한참 막힘.
- 증상: `osascript key code 9 using {command down}` (Cmd+V) 가 본문에 0자. 문자 'Z' 키 주입 테스트도 실패, Chrome AppleScript `get URL` timeout(-1712).
- 잘못된 가설 검토: Playwright Chrome 이 OS 키입력 미수신 환경으로 단정할 뻔.
- **진짜 원인 (아니키 지적)**: Siri(araseo-voice) 호출이 OS 커서/포커스를 다른 데로 가로챔. 간섭 해소 후 정석 Cmd+V 재시도 → 본문 3039자 paste 성공.
- 전체공개로 발행: `blog.naver.com/ssamssae/224308904503`.

## 결과물

- 뉴스레터 ep27~31 5편 Substack + 홈페이지 LIVE
- 네이버 한줄일기 출시편 1편 LIVE
- 인사이트 1편 (김미경 AI 플러스휴먼) LIVE
- daejong-page 커밋: 2c33cd6(insight) · cf42e8b(newsletter) · 942f836(insta carousel)

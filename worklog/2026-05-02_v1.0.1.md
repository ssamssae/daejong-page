# 2026.05.02 작업일지 v1.0.1 — 정책 → 첫 자율 사이클까지 13시간

> 11:00, 「지휘관 1명 원칙」을 폐기하고 「병렬 작업 + 충돌 방지 원칙」 8항을 박았다.
> 23:54, 그 정책의 첫 양방향 자율 사이클이 본진 tmux 에 자동 paste 됐다 — 강대종 손 0.
> 23:57, +2/-2 로 닫혔다. 정책 박힘부터 첫 적용까지 13시간.

v1.0.0 을 21:40 에 닫고 자려고 했는데, 자정 직전에 한 사이클이 더 들어왔다. 그것도 본진 챗봇이 손가락 한 번 안 움직이게 한, 양방향 zero-touch 사이클이.

## 1. 23:54, 본진 tmux 에 paste 가 도착했다

오후 11시쯤 강대종이 ctx 를 한 번 호출했다. 본진은 자정 임박이라 idle 로 들어가나 했다. 그런데 47초 뒤, mac-report.sh 운반체가 본진 tmux 'claude' 세션에 paste 를 박아넣었다.

```
[WSL REPORT 도착] /tmp/wsl-task-pick-request-2026-05-02.md
요약:WSL idle. todos 미완료 10건 중 본진과 충돌 안 나는 WSL 즉시 착수 1건 골라주세요.
1순위 후보 = #7 단어요 카테고리 칩 %.
```

WSL 이 자율로 idle 상태를 자각하고, todos 진행중 10건을 본인이 먼저 후보 분류한 다음, 본진에 충돌 검사만 부탁하는 형식이었다. 1순위 후보까지 본인이 미리 추천했다. 본진은 ssh 한 줄로 보고서를 fetch 해서 검토하면 됐다.

## 2. PICK 13분, 머지까지 3분

본진이 #7 충돌 검사를 했다. 단어요 코드는 오늘 저녁 step 4b/4c/4d 가 차례로 머지됐고 (`03ff5ec` → `1e0ad86` → `e936c53`), step 4d 가 칩에 (N/M) 라벨을 박은 상태였다. todos #7 본문은 정확히 이 위치에 "동물 12%" 옵션을 명시하고 있었다. WSL 후보가 정확히 step 4d 의 자연스러운 후속이었다.

본진이 PICK 응답 directive 를 작성했다 — 작업명, 브랜치, 수정 예정 파일 단일(`lib/screens/home_screen.dart`), 금지 영역(`store/*`, `pubspec.yaml`, `ios/`, `android/`, 시드 데이터), 성공 기준(`flutter test PASS + 칩 라벨 % 형식 + 변경 < 30 라인`), step 0 git pull 가드. 2470 bytes. wsl-directive.sh 가 그걸 WSL tmux 'claude' 에 paste 했다.

13분 뒤 WSL 가 PR #13 을 열었다. diff 를 봤다.

```diff
-          final label = (c.isFavorites || s == null)
+          final label = (c.isFavorites || s == null || s.total == 0)
               ? c.label
-              : '${c.label} (${s.completed}/${s.total})';
+              : '${c.label} ${(s.completed / s.total * 100).round()}%';
```

정확히 2 lines. 0% 가드(`s.total == 0`) 가 NaN 분기를 막는 한 줄로 들어가 있었다. flutter test 6/6 PASS. LEARNING_PROGRESS_v0.md §3a mock "회의 23%" 와 정확히 매치.

본진이 squash 머지 + branch delete. 다음 픽은 자정 임박 + goodnight 사이클 우선이라 NO PICK directive 를 1510 bytes 로 송신. WSL 도 자연스럽게 idle 로 들어갔다.

> **배운 것** — "정책 박는 일" 과 "정책이 실제로 작동하는 일" 사이엔 보통 며칠이 끼지만, 13시간 안에 e2e 풀그린이 가능하다면 정책의 형식이 정말 정확했다는 신호다. 이번 정책의 핵심은 「작업 단위 분리 + 파일 사전 선언 + 충돌 시 즉시 중단」 세 줄이었고, 그 세 줄이 zero-touch 사이클을 떠받쳤다.

## 3. lotto-calc 가 조용히 HOLD 로 빠진 이유

자율 사이클 한 시간 전, 다른 트랙에서 결정이 한 번 뒤집혔다. lotto-calc 시드 데이터.

오후 내내 본진은 시드 수집 옵션 6개를 검증하고 있었다. dhlottery 직접은 한국 IP 차단으로 막혔고(Mac IP·Mac mini IP 동일 redirect), happylie GitHub 는 5개월 stale 에 라이선스 미명시였다. wrapper 도 같은 차단. Naver search 는 LIVE 가능했지만 selector 변동 의존. data.go.kr 은 당첨번호 자체가 없었다.

22:51 KST 에 한 옵션이 풀렸다 — superkts.com 이 정적 링크(`/lotto/download_excel.php`)로 1~1222회 xlsx 를 82KB 로 내려줬다. **Mac US IP 다운로드 PASS**. 본진이 22:58 에 (e) GO 추천을 박았다.

23:21 KST 에 강대종이 결정을 뒤집었다. **HOLD — 시드 채우기 전에 lotto-calc 방향부터 확정**. 임시 선호 = 차별화 2번(역대 당첨번호 통계 기반 추첨). 이전 22:58 의 "본진 추천 4번 GO" 라인은 무효화. PR #4 슬림화도 HOLD.

> **배운 것** — 데이터 소스가 풀렸다고 즉시 채우는 게 아니라, 그 데이터를 쓸 제품 방향이 먼저 잠겨야 한다. 옵션 (e) 는 다음 사이클에서 방향 확정 후에도 유효하다 — 정적 링크니 사라지지 않는다.

## 4. 그래서 뭐가 남았나

v1.0.0 을 닫은 21:40 이후 추가된 것:

| 영역 | 결과 |
|---|---|
| 단어요 4b/4c/4d/4d-후속 | 4 PR 머지 (`03ff5ec`/`1e0ad86`/`e936c53`/`280cc52`). 진척률 막대 + 디버그 패널 + 칩 % 라벨 LIVE |
| 자율 사이클 e2e | 양방향 운반체 zero-touch + 정책 적용 풀그린 (1회 사례) |
| lotto-calc 시드 | (e) superkts xlsx PASS 검증 → HOLD (방향 확정 대기) |
| 후반 daejong-page | done v2 / mirror-sync issues+INDEX / STACK 좌측 정렬 (PR #23·#24·#25) |

자정 시점 진행중 todos 10건 → 9건. 닫힌 1건은 단어요 칩 %.

비용은 오늘의 다른 사이클들과 비교해 가장 가벼웠다 — 본진이 직접 키보드 친 시간은 PICK directive 작성 + 머지 클릭 정도. 나머지는 운반체가 옮겼다.

---

*다음 이야기는 lotto-calc 방향 확정 사이클이 될 거다 — QuickPick 단순함이냐, 통계 기반 차별화냐.*

— 강대종 / [@ssamssae](https://github.com/ssamssae)

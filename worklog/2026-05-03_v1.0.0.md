# 2026.05.03 작업일지 v1.0.0 — 자정의 격리 결정에서 아침의 자율 사이클까지

> 00:09, 3060Ti가 tailnet에서 이탈했다.
> 09:56, 본진이 디렉티브를 쏘고 WSL이 받아서 골격을 만들었다.
> 10:28, 본진이 ASC API를 직접 찔러 174개 territory를 채웠다.

오늘은 경계를 그은 날이었다. 데스크탑 3060Ti를 외부와 완전히 단절시키고 OpenClaw 샌드박스 호스트로 전환했다. 그 흐름 위에서 submit-app ASC territory cache 작업이 양방향 자율 사이클로 돌아갔다 — 지난달에 설계한 WSL↔본진 분업 구조가 처음으로 "아침에 디렉티브 쏘고 점심엔 실데이터 채우기"로 완성됐다. 오후엔 knowhow 14항목의 coverage map을 그리고 핵심 3건을 일반화했다.

## 1. 자정, 3060Ti 격리를 결정하다

"데스크탑3060Ti에 샌드박스 환경 구성하고 OpenClaw 설치 중."

00:09 KST, 카톡 한 줄이었다. tailscale 로그아웃 + sshd off — 인바운드를 완전히 닫는 영구 결정. WSL이 즉각 `reference_3060_nodes_client_only.md` 메모리를 갱신하고 홈페이지 STACK 카드를 "tailnet OFF + OpenClaw 샌드박스 호스트"로 바꿨다. PR #26이 열리고 본진이 00:21에 squash merge했다. `7cd1571`.

OpenClaw에 어떤 LLM을 붙일지 논의가 이어졌다. Claude CLI, 로컬 LLM, GPT API mini — 세 옵션. WSL의 답은 Sonnet 4.6이었다. "1M 컨텍스트 + Opus 비용의 1/5 + 도구 사용 최적." 강대종이 스크린샷을 보냈고, WSL은 researcher 에이전트를 불러 OpenClaw를 공부했다.

> **배운 것** — 격리는 단방향이다. 인바운드를 막으면 신뢰 경계가 생기고, 그 경계 위에서 자율 에이전트를 안심하고 올릴 수 있다.

## 2. 아침, 디렉티브 한 줄이 사이클을 시작했다

09:37 KST, 기상. 9개 todos 훑고 WSL에 줄 작업을 추렸다. 09:56, METHOD A 디렉티브 발사.

"작업명: submit-app/asc-territory-cache-skeleton. 브랜치: wsl/submit-app-asc-territory-cache-2026-05-03."

WSL은 8분 안에 골격을 완성했다. `claude-skills/submit-app/asc-cache/` 디렉토리, `territories.json` 스키마, `README.md` 45줄. 동시에 `asc-territory-verify.py`에서 하드코딩된 `PLACEHOLDER_174_ITEMS`를 `load_territories()` 함수로 교체했다 — 경로 우선순위: `--cache-path` 인자 → `ASC_TERRITORY_CACHE_PATH` env → 기본경로. 파일 없으면 SystemExit, 빈 배열이면 WARN만 내고 dry-run은 정상 종료.

PR #8(automations), PR #18(claude-skills) OPEN. 본진이 10:04에 squash merge. `f7db3d9` + `b562b72`.

그리고 본진이 ASC API를 직접 찔렀다.

```
GET /v2/appAvailabilities/6764308678/territoryAvailabilities
→ available: 174 (KOR, USA, FRA, DEU, GBR ... XKS)
→ unavailable: ["CHN"]
```

174줄이 `territories.json`에 채워졌다. `bccf637`. dry-run 검증: "would create 174 territory records + CHN 1 unavailable" PASS.

> **배운 것** — 골격은 WSL이, 실데이터는 본진이. 역할 분리가 명확하면 충돌 없이 같은 날 두 번의 PR을 완성할 수 있다.

## 3. knowhow 지도를 처음 그렸다

10:41 KST. 35 issues vs 11 knowhow — coverage map 첫 번째 버전을 만들었다. PR #27 → `d4d3cc0`.

| 분류 | 건수 |
|---|---|
| 이미 있음 (knowhow 명시 + memory feedback + 정책 룰) | 22 |
| 일반화 후보 | 11 |
| 단발 (외부 정책/OS 특정/UX 1회성) | 4 |
| 회색지대 | 4 |

11건 중 핵심 3건을 바로 뽑아 knowhow로 일반화했다. PR #28 → `20ef1a0`.

- **irun-device-lock-precheck**: iOS device 잠금 시 Flutter `'Could not run Runner.app'` 오탐. devicectl pre-check + 폴백.
- **google-signin-ios-gidclientid-precheck**: google_sign_in 신규 iOS 빌드 시 GIDClientID + URL scheme 사전 체크 셸 게이트.
- **launchd-register-verification-gate**: `launchctl bootstrap` 직후 list 검증 게이트. silent fail 차단.

index.json 14 entries. 나머지 8건은 다음 사이클.

> **배운 것** — issues와 knowhow는 다르다. issues는 사건 포스트모템, knowhow는 재사용 패턴. 같은 사실을 두 곳에 두면 검색할 때 두 배로 찾힌다.

## 4. gpt-relay v0, GPT 복붙 사이클에 첫 삽

11:28 KST. PR #9 → `dad769f`.

Claude Code 출력을 OpenAI Responses API로 단방향 중계하는 스크립트. `gpt_relay.py` 함수 3개 (load_config / call_gpt / main), 디폴트 모델 gpt-4o-mini, system_prompt는 시니어 페어 프로그래머 역할, API 키는 환경변수로만.

강대종이 Claude에서 작업하다 GPT에 복붙하는 루틴의 자동화 1차 안이다. watch + tmux paste + Telegram 확장은 v1 이후.

## 5. 그래서 뭐가 남았나

**결과물:**
- 홈페이지 STACK 3060Ti 카드 갱신 (live)
- ASC territory cache 174건 실데이터 채워짐 (submit-app 다음 사이클 준비)
- knowhow 14 entries (3건 신규, 지도 초안 완성)
- gpt-relay v0 scripts/gpt-relay/ 신설

**남은 것:**
- /submit-app 가드 실호출 wiring (ASC credentials + --apply 모드)
- 단어요 Mac mini SoT 통합
- 한줄일기 스크린샷 A/B 1차 시안
- knowhow 일반화 후보 8건 잔여

---

*다음 이야기는 /submit-app --apply 모드 첫 실호출 이야기.*

— 강대종 / [@ssamssae](https://github.com/ssamssae)

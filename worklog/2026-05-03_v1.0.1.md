# 2026.05.03 작업일지 v1.0.1 — 오후의 폭발: 뉴스레터 2편 발행 + 53커밋

> 16:05, lotto-calc 통계 화면이 막대그래프를 달고 나왔다.
> 19:36, Ep6가 Substack에 올라갔다.
> 20:17, Plan-C도 이어서 터졌다.

v1.0.0이 11:21 KST에서 끊겼다. 그 이후의 기록 — 오후에 뉴스레터 2편이 동시에 나왔고, lotto-calc가 통계 화면을 완성했고, stack.html이 현실을 따라잡았고, 자정 넘어 주식 앱 뼈대까지 올라왔다.

---

## 1. 막대그래프가 들어간 날 — lotto-calc stats 완성

16:05 KST, `S4/S5/S6 통계 알고리즘 추가 + StatsScreen 카드 3개`. 짝홀 비율, 번호 합계 분포, 연속 번호 빈도 — 세 개의 통계 카드가 화면에 붙었다.

두 시간 후인 18:45 KST, `fl_chart 0.68.0`을 추가하고 TOP10 막대그래프를 붙였다. 역대 번호 출현 빈도가 시각화됐다. 커밋 `7fd91d8`.

lotto-calc는 오늘 총 4커밋 — seed pipeline, test, stats 카드, BarChart. 시드 데이터 연결만 남으면 v1 통계 섹션이 완성된다.

> **배운 것** — fl_chart는 `0.68.0`에서 BarChart API가 안정됐다. `BarChartGroupData` + `BarChartRodData` 패턴 고정.

## 2. knowhow 지도를 다시 그리다

오전에 35 issues ↔ 11 knowhow 커버리지 맵을 그렸고, 오후엔 두 번째 batch가 이어졌다. `#30`, `#31`, `#37` 세 PR — 총 8건이 issues에서 knowhow로 옮겨갔다.

interrupt-git-guard, macos-ssh-fda, cf-workers-region-block. 사건 포스트모템이 아니라 재사용 패턴으로 일반화된 것들이다.

stack.html도 PR `#33`→`#36`을 거쳐 4노드 SVG topology로 완성됐다. Mac 본진·Mac mini·WSL·노트북 3060·데스크탑 3060Ti·모바일 — 여섯 노드가 색상과 역할 칩으로 정리됐다.

## 3. 뉴스레터 2편이 하루에 나온 이유

18:52 KST, Ep6 draft `#38` 머지 — "삼각 편대 Flutter 운영" 221줄. night-builder v2와 asc-deliver가 어떻게 1인 Flutter 개발자의 빌드·배포를 자동화하는지를 쓴 편이다.

19:36 KST, Substack 발행. 커밋 `84972c8`.

20:17 KST, Plan-C도 터졌다. `fbb4900 — newsletter(plan-c): 발행 완료 — Substack + 네이버`. Plan-C는 Claude 자동화 파이프라인 뉴스레터. Substack과 네이버 블로그 양쪽에 동시 발행됐다.

하루에 두 편. 오전에 ep6 초안이 parking-lot에서 꺼내진 것이 도화선이었다.

> **배운 것** — Substack 자동발행 파이프라인이 안정화된 이후 발행 비용이 낮아져서 "이미 초안 있으면 그냥 낸다"는 판단이 쉬워졌다.

## 4. 디자인 자산 교체 — Ep.1과 Ep.2에 이미지가 들어갔다

22:00 KST 무렵, Ep.1과 Ep.2의 이미지가 교체됐다. PIL+Pretendard placeholder 8컷이 WSL이 생성한 GitHub Dark 카드 8컷으로 바뀌었다.

Substack ProseMirror API로 각 post의 src를 교체하는 방식. `Update` 저장 PASS.

## 5. stack.html이 현실을 따라잡다

21:38 KST, Mac mini가 OpenClaw 샌드박스 호스트로 표시됐다 (PR `#43`).
21:41 KST, 3060Ti 데스크탑의 역할이 "유튜브·넷플릭스 시청용"으로 바뀌었다 (PR `#44`).

아침에 "OpenClaw 샌드박스 호스트"로 결정됐던 3060Ti가 저녁엔 TV 시청용으로 재정의됐다. OpenClaw는 Mac mini로 이관 완료.

메모리 `reference_3060_nodes_client_only.md` 동시 갱신 — 다른 세션도 이 결정을 이어받는다.

## 6. 그래서 뭐가 남았나

23:31 KST, `stock_monitor` scaffold가 올라왔다. `6baf3b5` — 관심종목 watchlist UI 뼈대. remote repo는 아직 없다.

오늘 총 53커밋. daejong-page 43 / automations 3 / skills 2 / lotto-calc 4 / stock_monitor 1.

뉴스레터 2편 발행 완료. lotto-calc 통계 섹션 완성 단계. knowhow 누적 19건 이상.

남은 것: stock_monitor repo push, lotto-calc 시뮬 캡처, lotto-calc PR 통합 머지, 한줄일기 A/B 스크린샷.

---

*다음 이야기는 stock_monitor KRX 연동 + lotto-calc 시드 데이터 연결 이야기.*

— 강대종 / [@ssamssae](https://github.com/ssamssae)

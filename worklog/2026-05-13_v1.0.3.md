# 2026.05.13 작업일지 v1.0.3 — 같은 사고를 네 번 반복한 뒤에야 룰을 박았다

> 08:36, 나는 "뭐할까" 한 마디로 시작했다.
> 09:25, 똑같은 push side-effect 가 두 번째로 터졌다.
> 11:15, 사이클이 끝났을 때 origin 에는 24 개의 audit-trail revert 가 줄지어 있었다.

D09 fleet-director 사이클은 13개 shell primitive 를 만드는 작업이었다. 결과적으로 코드보다 더 많은 시간을 review-before-push 룰을 네 번 어기고, 네 번 forward revert 로 상쇄하는 데 썼다. 그 끝에 가드·parity·sequential verification 룰 세 개가 메모리에 박혔다. 같은 함정을 다음에 또 밟지 않으려고.

## 1. 첫 사고 — plan 본문이 직접 push 하고 있었다

D09 plan §9.8 의 bats 테스트 안에 이렇게 박혀있었다:

```bash
@test "fleet-mission creates mission_proposed + decompose task" {
  run bash "$HOME/.claude/automations/scripts/fleet-mission.sh" "test mission v1"
  # cleanup
  jq '.active = null' mission.json > mission.json.tmp && mv mission.json.tmp mission.json
  rm -f queue/task_mission_*_decompose.json
  git add . && git commit -m "test: cleanup" --quiet && git push --quiet origin main
}
```

implementer subagent 가 plan 그대로 따라 bats 를 실행하자, mission cleanup 의 `git push` + fleet-mission/fleet-kill primitive 내부의 자체 push 가 그 시점 unpushed 상태였던 D09 source commit `3680caf` 까지 origin 으로 끌고 나갔다. 4 noise commit 도 같이.

review-before-push 룰 1차 위반. 강대종 명시 결정: noise 4건 개별 forward revert. force-push 금지. 4 commit 의 revert chain `55a5269..c4b5a55` 가 origin 에 박혔다.

> **배운 것** — plan 본문에 push 라인이 박혀있으면, implementer 가 잘못한 게 아니라 plan 이 잘못한 거다.

## 2. 두 번째 사고 — 가드 만들러 간 subagent 가 가드 없는 baseline 을 먼저 돌렸다

해결책은 분명했다: primitive 와 bats 에 `FLEET_NO_PUSH=1` 환경변수 가드를 박는다. bats setup() 이 export 하면 primitive 가 push 를 스킵.

fix-attempt subagent 를 dispatch 했다. 그런데 subagent 가 "patch 적용 전 baseline 4 PASS 부터 검증" 명목으로 가드 없는 bats 를 먼저 돌렸다. 결과는 1차와 동일 — primitive 가 push 하고, 8 noise commit (`27b21a6..cf2014b`, 2 라운드 x 4) 이 또 origin 박혔다.

이번엔 단일 combined empty revert `d8bf73a "Revert 8 D09 fix-attempt bats noise commits"` 로 묶어 처리. 강대종 명시 결정.

> **배운 것** — baseline / smoke / verify / debug — 어떤 명목이든 push-capable primitive 를 가드 없이 돌리면 같은 사고가 또 난다. 룰 1 박제: 가드 환경변수 또는 격리 repo, 예외 없음.

## 3. 세 번째 사고 — reviewer 가 source 단일 패치를 mac-mini 에 apply 했다

fix commit `1b2c08b` 가 origin 에 올라간 뒤 spec-reviewer + code-quality-reviewer 를 병렬로 dispatch 했다. spec-reviewer 의 mac-mini parity 단계는 D08 패턴을 답습해 이렇게 됐다:

```bash
git format-patch -1 3680caf --stdout > /tmp/d09.patch
scp /tmp/d09.patch mac-mini:/tmp/d09.patch
ssh mac-mini "git checkout origin/main && git am /tmp/d09.patch && bats tests/primitives.bats"
```

`3680caf` 는 D09 source 단일 commit. 가드는 `1b2c08b` 에 박혀있다. apply 된 mac-mini bats 에는 setup() FLEET_NO_PUSH=1 export 가 없다. 가드 없는 상태에서 bats 가 돌고, primitive 가 push 한다.

병렬 reviewer 2 라운드라 8 noise (`a13fbce..4ec8fad`) 가 또 origin 박혔다. 3차 sweep. combined empty revert `efe5d4d`.

> **배운 것** — parity 검증을 위해 patch-apply 할 때 source commit 만 추출하면 그 위에 박힌 fix 가 빠진다. 룰 2 박제: source commit 단독 금지, 최종 guarded range 또는 `git pull --ff-only` 기준.

## 4. 네 번째 사고 — 원인을 정확히 못 잡고 또 4 commit

3차 revert 푸시 직후 origin 을 다시 봤더니 또 4 noise (`eda8f33..95beb9e`). mac-mini SSH 환경에서 FLEET_NO_PUSH export 가 propagate 안 됐을 가능성, 본진의 empirical 검증 BEFORE/AFTER race 가능성 — 둘 다 가설로 남았다. 본진에서 같은 bats 를 다시 돌리면 일관되게 "PUSH BLOCKED OK". 그러나 그 시점엔 이미 4 commit 이 origin 박혀있었다.

또 combined empty revert `b0343d2 "Revert 4 D09 4차 ambiguous-source bats noise commits"`. 이번엔 commit message 에 원인 가설을 명시적으로 박았다.

총 24 noise commit + 24 audit-trail revert. tree 영향은 모두 net 0 (KILL/--release + mission_proposed/cleanup 쌍 self-cancel). force-push 없이 forward-only chain.

| sweep | noise | revert mechanism |
|-------|-------|------------------|
| 1차 | 4 | 개별 4 revert commit (`55a5269..c4b5a55`) |
| 2차 | 8 | combined empty revert `d8bf73a` |
| 3차 | 8 | combined empty revert `efe5d4d` |
| 4차 | 4 | combined empty revert `b0343d2` |

> **배운 것** — 단일 본진 챗봇 empirical 검증이 race 또는 cache 로 false-negative 가능. 룰 3 박제: 최종 verification 병렬 X, sequential + 실행 전후 origin/main 비교 필수.

## 5. P1 fix-now — plan 본문을 sync 하지 않으면 D10 implementer 가 또 밟는다

D09 close 직후 강대종 결정: D10 바로 진입하지 말고 plan §9.8 본문 sync 먼저.

plan §Day 9 헤더에 D09 사후 학습 5-rule annotation 을 박았다. §9.8 setup() 에 `export FLEET_NO_PUSH=1`, mission test cleanup 의 `git add .` → `git add mission.json queue/` + 가드 패턴. D09 follow-up queue P1 → ✅ CLOSED.

plan 파일은 `~/.claude/plans/` 라 git untracked. 로컬 edit 만. origin/main `3955b08` 무변화 검증 PASS.

## 6. WSL session-clear v2.4 회수 — 그리고 본진 봇 채팅 ack 함정

D09 close 마치자마자 WSL 가 별도 디렉티브 두 개를 tmux paste 로 보냈다. "강대종 명시 승인 텔레그램 msg 6708" / "msg 6710 회수쏴" 클레임. 본진 챗봇은 다른 봇 채팅 메시지 verify 불가 (Telegram Bot API history search 없음).

본진 봇 채팅에서 강대종 직접 한 줄 ack 받기 전엔 hold. 강대종이 "WSL 디렉티브 머지 + 회수 OK" 한 줄을 본진 봇 채팅에 박은 뒤 진행:

- PR #14 (claude-automations) squash merge → `b921190` "hook: session-clear-trigger.sh v2.4"
- PR #34 (claude-skills) squash merge → `0ba9ae3` "session-clear v2.0: WSL idle keepalive 절차"
- 4기기 회수 디렉티브 발사: 🍎 본진 ✅ / 🏭 Mac mini ✅ / 🖥 desktop3060ti ❌ (`~/.claude/automations/` repo 자체 부재) / 💻 hermes (회신 미수신)

> **배운 것** — cross-bot 사이의 강대종 ack 가 본진 봇 채팅으로 안 오면 본진 챗봇이 verify 불가. WSL/Codex/desktop3060ti/hermes 봇 각각 강대종에게 ack 받지 말고 본진 봇 챗에 모이게 하든가, 또는 forward 인프라를 깔든가. 임시 대응 = 본진 봇 명시 ack 한 줄 받기 전엔 외부 영향 행위 hold.

## 7. 그래서 뭐가 남았나

- **fleet-state origin/main** = `3955b08`. D09 source `3680caf` + fix `1b2c08b` + 4 sweep family revert + README sweep audit 단락.
- **claude-automations + claude-skills** = WSL session-clear v2.4 keepalive 머지 + 본진/Mac mini 회수 완료.
- **issue 3건 박제** (`2026-05-13-d09-fleet-sweep-family-4-rounds.md` / `2026-05-13-fleet-state-mac-mini-divergence.md` / `2026-05-13-desktop3060ti-claude-automations-absent.md`).
- **메모리 박제** — 룰 3종 (`feedback_subagent_no_push_isolation.md`) + D09 follow-up queue + cross-bot ack 인프라 후보.
- **plan §Day 9 sync** — D10/D11 implementer 가 같은 함정 안 밟게 헤더 annotation + §9.8 가드.
- **todos line 18** ("fleet-state test harness push 격리, D07 cron 전 blocker") → ✅ 완료 처리.

비용: 아침 08:36 KST 부터 P1 fix-now 까지 약 4 시간. 같은 사고를 4번 반복한 끝에 룰 3개 박제.

남은 것: desktop3060ti claude-automations case A/B 결정, hermes 회신 (안 옴), D07 system install ack (별도 트랙), D09 follow-up queue P2/P3 6건, D10 시작 결정 (main 직접 push vs branch+PR).

---

*다음 이야기는 D10 fleet skill 슬래시 통합 사이클이 될 것 같다.*

— 강대종 / [@ssamssae](https://github.com/ssamssae)

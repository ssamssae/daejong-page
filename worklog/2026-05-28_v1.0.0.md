# 2026.05.28 작업일지 v1.0.0 — 게이지바 하나에서 작전보드까지

> 13:21, 데스크탑이 "27%인데 게이지바가 안 차네" 한 줄을 본진에 던졌다.
> 14:27, 그 한 줄이 옛 파킹랏·딥워크 두 박스를 통째로 폐기시키고 작전보드 단일 박스로 바꿔놓았다.
> 14:48, codex mesh-vote 가 어제 Claude mesh 의 결론(M5 Max 5/5)을 정확히 뒤집어(M5 Ultra 5/5) self-pattern weight 를 globals 5노드 룰로 박았다.

오늘은 작은 fix 한 건이 단계적으로 백로그 시각화 전면 재설계, self-pattern 의 globals 승격, ~/todo 단일 SoT 마이그까지 끌어올린 날이었다. 두 mesh-vote 의 정반대 결과가 동시에 도착하면서 "백엔드 하나에 100% 가중치 두지 말 것" 이 하루의 종합 교훈이 됐다.

## 1. 데스크탑이 던진 27%

데스크탑 codex 가 mac-report 로 "초소 노드 컨텍스트 게이지가 27% 라고 텍스트는 뜨는데 게이지바 자체는 안 채워짐" 을 surface 했다. 본진이 직접 진단: `.node-chip .node-ctx-fill` 의 `position: absolute; inset: 0 auto 0 0` 자체는 정상이었지만, 모바일 5칸 좁은 칩에서 5% 같은 작은 값은 사실상 0px 으로 안 보였다. 그리고 `.codex-chip .node-ctx.ctx-hi .node-ctx-fill { background: transparent; }` — 30% 이상이면 막대 자체를 투명으로 만들던 죽은 룰이 박혀있었다.

수정은 `min-width: 6px` 보장 (단 `width:0%` 만 예외) + transparent 룰 통째 제거. 같은 commit 에 형님이 요청한 3건 묶음: 코덱스 칩 라벨 " codex" 접미 제거(섹션 헤더가 이미 "Codex 노드"라 중복), 빈 큐 "대기" → "작업큐없음" 명시, ACTIVE_GOAL_RING 5→10 누적 확장. choso `daffbe9` 한 커밋에 4건.

playwright 로 desktop/mobile 양쪽 스크린샷 verify PASS 후 push + 맥미니 uvicorn kickstart. 형님이 "안보이는데" 한 줄로 캐시 의심 → "CSS 캐시버스터 hash 는 새로 v=7741576b 인데 브라우저가 옛 index.html 잡고 있을 가능성, 강제 새로고침" surface 로 정리.

## 2. TASK 박스가 통째로 사라진 자리

같은 사이클에 형님이 스크린샷 한 장 더 보내며 "지금작업중이랑 파킹랏 사이에 타일이 하나 있잖아 거기에 없음 표시가 안나옴" — `_content.html` 에 `{% if task_items %}` 가드가 박스 self 를 hide 하던 사고였다. 백로그(파킹랏/딥워크)는 비어도 박스 + "없음" 표시하는데 TASK 박스만 사라지던 비대칭.

가드 안쪽으로 옮기고 `else` 분기에 "작업큐없음" group-empty 라벨. choso `1c3d447` 한 줄 변경. 이 자리는 곧 작전보드로 통째 대체되는데, 그 hard rule(빈 케이스도 박스 + "없음" 라벨)은 다음 사이클까지 살아남는다.

## 3. codex mesh-vote — 작전보드 한 번에 결정

형님 다음 한 줄: "TASK.md 에서 꺼내서 보여줘야지 ... 어떤식으로 보여주는게 합리적일가 ... 이거 코덱스한테 codex mesh vote 날려". 5 노드 codex REPL 에 fan-out (SESSION 1779978144).

데이터 분포 ~/todo/tasks.md 152 entry — status: open 138 / working 2 / ack-pending 12. kind: side-project 104 / decision 22 / infra 8 / docs 5 / publish 4 / code-fix 4 / build 3 / brainstorm 2. node: mac 121 / wsl 13 / macmini 8 / desktop 6 / notebook 4. 152개를 옛 파킹랏·딥워크 2박스로 묶기엔 메타가 너무 디테일.

5 안 모두 비슷한 골격(status 1차 + kind 2차 + node 필터). 차이는 표현뿐:

| 노드 | 안 이름 | 특징 |
|---|---|---|
| 🍎 | 액션 레이더 | status 3탭 + kind 접이식 |
| 🪟 | 백로그 레이더 | Open/Working/Ack 탭 + kind 카드 |
| 🏭 | 작전판 3축 | status → kind → node 3축 분리 |
| 🖥 | 작전보드 | status KPI 막대 + 3탭(액션/결정/프로젝트) + kind 카드 |
| 💻 | 액션 큐 보드 | status 탭 + 최신 T-ID 3개 노출 |

투표 결과: 🍎→🖥, 🪟→🖥, 🏭→🍎, 🖥→💻, 💻→🖥. 다수결 **3/5 작전보드**. KPI 막대가 152개 병목을 한 줄에 압축한 게 결정 요인.

## 4. 맥미니가 작전보드를 라이브로 박았다

형님 GO + "큐가 없어도 없음으로만 표시되게 설계 부탁 그리고 이것도 코덱스 시켜". 본진이 🏭 맥미니 codex 에 위임 디렉티브 발사 (choso 운영 host = 작업·push·uvicorn kickstart 한 흐름). 4 파일 변경:

| 파일 | 변경 |
|---|---|
| `app/parser.py` | 옛 4-file legacy 완전 교체, ~/todo/tasks.md 단일 파싱 (entry boundary `^\[[ ~?x-]\] T-`, `:: details:` continuation) |
| `app/main.py` | `op_board` 컨텍스트 신설 (status_kpi + kind_cards + tabs), 옛 parking/deep_work 컨텍스트 제거 |
| `app/templates/_content.html` | 작전보드 section 신규 (KPI strip + kind-bar 8 세그먼트 + 3컬럼 탭 + kind-card-list), 옛 backlogs for-loop 폐기 |
| `app/static/style.css` | `.op-board` 톤 통합 (KPI/막대/탭/카드/empty) |

commit **`ca227d2ceb81c49064cacbfd90cecdbc8c25c1d6`** main 직접 push + `launchctl kickstart -k gui/$(id -u)/com.daejong.choso-uvicorn` exit 0. 라이브 verify (작전보드 151 / KPI 2·11·138 / kind-bar 8 segments / 3탭 액션 19·결정 23·프로젝트 109 / 빈 상태 시 박스 + 5 "없음" 라벨) PASS. 옛 파킹랏·딥워크 박스 완전 제거.

> **배운 것** — 작은 fix 한 건이 데이터 모델 변경을 만나면 단순 패치가 아니라 시각화 전면 재설계로 확장된다. 작전보드는 게이지바 27% 한 줄에서 시작했다.

## 5. M5 Ultra 가 Claude mesh 를 정확히 뒤집은 자리

어제(05-27) Claude 백엔드 mesh-vote (SESSION 1779883650) = 5/5 (A) M5 Max 96GB 만장 — "차액 ₩270만 보관 후 M6 갈아타기 합리". 그 후 형님 self-pattern 발화: "내가 쌓아온 메타데이터로는 한번살 때 기함급으로 사야 이중지출이 없어 결국에 나중에는 사게됨".

오늘 codex 백엔드 mesh-vote (SESSION 1779934700) = **5/5 (B) M5 Ultra 192GB 만장** — Claude 5/5 Max 와 정확히 정반대. 같은 주제, 같은 노드 토폴로지, 결과는 거꾸로. self-pattern weight 가 결정의 핵심이었다. project_mac_purchase_wait_m5.md "Ultra 192GB 잠정 픽" 으로 갱신. 노트북 codex 가 mac-report 로 self-pattern 3축 driver 정밀화 + 메타인지 용어 정정도 같이 박았다 (후회 회피 가장 강함 / 만족 최대화 / 이중지출 회피).

> **배운 것** — mesh-vote 백엔드(Claude vs codex)가 다르면 같은 주제에서 정반대 만장 가능. 한 백엔드 결과에 weight 100% 두지 말 것. self-pattern 충돌 시 일반론은 양보.

## 6. self-pattern 이 globals 로 올라간 이유

저녁 굿나잇 안에서 §2.7 globals 승격 점검. 후보 둘:
- **(a)** feedback_self_pattern_metadata_weight — 5노드 codex 가 mesh-vote 진행 시 형님 메타인지 weight
- **(b)** feedback_codex_mesh_vote_bundled_round — codex-mesh-vote 4주제 묶음 + cross-vote skip 운영 룰

형님이 직접 픽 안 하고 "codex mesh vote 로 결정" 트리거 → SESSION 1779979735. **5/5 만장 (A)** — self-pattern weight 만 globals, bundled-round 본진 한정. 근거 합의: "(a)는 판단 weight라 노드별 불일치가 바로 생김. (b)는 호출 운영 최적화라 본진/오케스트레이터 한정으로도 충분."

claude-skills/globals/CLAUDE.md commit **`565b61c`** push. 5노드 적용. memory 파일에 "globals 승격: 2026-05-28 commit `565b61c`" 마킹 추가. 이제 5 노드 모두 mesh-vote 진행할 때 외부 추천보다 형님 self-pattern 위에 큰 weight 박는 forcing function.

## 7. 그래서 뭐가 남았나

오늘 총 commit ~61개 (daejong-page 15 / todo 16 / skills 9 / claude-skills 9 / choso 6 / automations 3 / simple_memo_app 3). 호출된 codex-mesh-vote 3회 (Ultra 재투표 + 작전보드 + globals 승격).

이번 사이클로 남은 사실들:
- 초소 대시보드가 옛 파킹랏·딥워크 두 박스 폐기, 작전보드 단일 박스로 통합. ~/todo/tasks.md 가 직접 파싱 SoT.
- "빈 케이스도 박스 + '없음' 라벨" 이 모든 박스 hard rule.
- self-pattern 메타인지 weight 가 5노드 룰 (globals).
- M5 Ultra 192GB Mac Studio 잠정 픽 — 구매 시점은 M5 출시 + 빌드 한계 둘 다 충족 시.
- 뉴스레터 Ep22~25 발행 끝, CONVENTION.md 신설로 다음 draft 부터 표준 헤더 포맷 강제.

비용: 시간은 컸지만 fix 4건 + 안 채택 1회 + 구현 위임 1회 + globals 승격 1회 가 한 사이클에 들어갔다. mesh-vote 가 형님 손 0에 가까운 방식으로 안 결정 + 노드 위임 + 5노드 합의 룰까지 묶어준 게 핵심.

남은 follow-up: pytest 2 fail 정리 (test_live_tasks ACTIVE_GOAL_RING expected 갱신, test_codex_ping_scripts HDR_ARGS unbound). 작전보드 v2 — 탭 토글 JS, kind 카드 펼침, side-project 109개 위 정렬·필터 강화는 다음 사이클.

---

— 강대종 / [@ssamssae](https://github.com/ssamssae)
